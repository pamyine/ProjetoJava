

## 🧩 Como rodar o projeto
1. Baixe ou clone este repositório.
2. Importe o arquivo `sistema_escola.sql` no MySQL Workbench (Data Import → Import from Self-Contained File).
3. Abra o projeto no NetBeans (`File → Open Project`).
4. Configure a classe de conexão (`ConexaoDB.java`) com o seu usuário e senha do MySQL.
5. Execute o projeto a partir da classe principal (`Main.java`).

usuario: root
senha: PAMSQLa8741!@
